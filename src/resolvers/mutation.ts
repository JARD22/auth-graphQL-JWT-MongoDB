import {IResolvers} from '@graphql-tools/utils'


// 2 Crear los resolver para cada una de las definiciones

const mutationResolvers:IResolvers = {
    Mutation: {
}
}

export default mutationResolvers;